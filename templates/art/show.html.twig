{% extends 'base.html.twig' %}

{% block title %}Art{% endblock %}

{% block body %}
    <h1>Art</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ art.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ art.Name }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ art.Img }}</td>
            </tr>
            <tr>
                <th>Artiste_name</th>
                <td>{{ art.artisteName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ art.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_art_index') }}">back to list</a>

    <a href="{{ path('app_art_edit', {'id': art.id}) }}">edit</a>

    {{ include('art/_delete_form.html.twig') }}
{% endblock %}
