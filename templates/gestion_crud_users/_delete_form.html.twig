<form method="post" action="{{ path('app_gestion_crud_users_delete', {'id': gestion_crud_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gestion_crud_user.id) }}">
    <button class="btn">Delete</button>
</form>
